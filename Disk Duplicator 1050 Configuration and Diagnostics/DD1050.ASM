
;
; Author:	E474
; Version: 	1.0
; Copyright (c) 2020, E474, Portugal.
; File:		DD1050.asm
;
; Configure and diagnose a Disk Duplicator 1050 
;  
;
; Handy system equates defined as part of ATASM
;

	.INCLUDE "OS.asm"

;
; Disk Duplicator 1050 Firmware jump vectors
;

	.INCLUDE "DDF_EQU.ASM"


;
; Equates for sizes, buffers and variables
;

;VERSION_STRING = "1.0"


SIO_READ_DATA			= $40
SIO_WRITE_DATA			= $80
SIO_NO_DATA				= $00

DD_FIRMWARE_COLDSTART = [$E000] - 1

NTRK	=	$0A

;
; Start of actual program area
;
									
	*= $3F00

COMMAND_CODE_BUFFER

DD_COLD_START_CODE

	JMP $E000
	
DD_RETURN_SUCCESS_CODE

	LDA #0
	STA $A0
	RTS	
	
DD_RETURN_FAIL_CODE

;
; returns SIO error code 144
;
; From https://www.page6.org/archive/issue_22/page_10.htm
; 
; Error-144 Device done error.
;
; This error occurs when you have issued a valid command to the peripheral but the device is unable to carry it out. For example, 
; you may have tried to write to a disk that is write-protected, or there may be no disk at all in the drive. 'Your Atari Computer' 
; implies that this error might also occur if the disk directory was damaged in some way.
;
;The cause of the error depends on the device, so check the command given and whether the device is prevented in some way from executing it.
;

	LDA #2
	STA $A0
	RTS	


DD_STEP_5_TRACKS



	; move to track zero, then move to track 5, then tracks 6, 7, 8, 9, 10

	JSR TRK0V
	
	

	LDA #5
	JSR SEEK_TO_TRACK
	LDA #6
	JSR SEEK_TO_TRACK
	LDA #7
	JSR SEEK_TO_TRACK
	LDA #8
	JSR SEEK_TO_TRACK
	LDA #9
	JSR SEEK_TO_TRACK
	LDA #10
	JSR SEEK_TO_TRACK

	LDA #0
	STA $A0
	RTS	

SEEK_TO_TRACK

; relies on TRACK being set, which is maybe contained in the command frame?

	STA NTRK
	JSR SKTKV
	RTS



	.INCLUDE "SYSEQU.M65"
	.INCLUDE "IOMAC.LIB"
	
	
;
; Strings and variables
;
	.INCLUDE "BINMATHS.ASM"
	.INCLUDE "TEXT.asm"
  
;
;
;
;
;
;
;
; Start here
; 

PROGRAM_INIT	

	LDA #0
	STA LMARGN											; set left margin to zero

	OPEN 2,4,0,"K:"									; open keyboard for reading


;
; main loop
;
GET_DRIVE_NUMBER

; Greeting, purpose, exit method

	BPUT 0, WELCOME_TEXT,WELCOME_TEXT_LENGTH
	
;	BPUT 0,DRIVE_SELECTED_MESSAGE, DRIVE_SELECTED_LENGTH
	BGET 2,DRIVE_NUMBER_CHAR,1  							; Read keystroke
    BPUT 0,DRIVE_NUMBER_CHAR,1  							; Echo keystroke


PROCESS_KEYPRESS

	LDA DRIVE_NUMBER_CHAR								; Validate drive number is in range 1 - 4
    CMP #'1
    BMI INVALID_DRIVE_NUMBER
    CMP #'5
    BMI VALID_DRIVE_NUMBER
INVALID_DRIVE_NUMBER								; not a valid drive

	BPUT 0, BAD_DRIVE_NUMBER_TEXT, BAD_DRIVE_NUMBER_TEXT_LENGTH	
	
	JMP GET_DRIVE_NUMBER							; try again
	
	
	
	
VALID_DRIVE_NUMBER

	SEC
	LDA	DRIVE_NUMBER_CHAR					; convert DRIVE_NUMBER_CHAR into actual drive number (1..4)
	SBC #'0		 							;
	STA DRIVE_NUMBER


; display main menu and get user selection	

DISPLAY_MAIN_MENU
	BPUT 0, MAIN_MENU_TEXT,MAIN_MENU_TEXT_LENGTH


; see if valid character entered

PROCESS_MENU_INPUT

	BGET 2,MENU_CHOICE_CHAR,1  							; Read keystroke

	LDA MENU_CHOICE_CHAR
	CMP #'1
	BMI PROCESS_MENU_INPUT
	CMP #'9
	BMI VALID_MENU_CHOICE

INVALID_MENU_NUMBER

	JMP PROCESS_MENU_INPUT							; try again


VALID_MENU_CHOICE

    BPUT 0,MENU_CHOICE_CHAR,1  							; Echo keystroke
	
;
	LDA MENU_CHOICE_CHAR
	CMP #'1
	BEQ CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY
	CMP #'2 
	BEQ UPLOAD_COLDSTART_CODE
	CMP #'3
	BEQ TURN_ON_TRACK_BUFFERING
	CMP #'4
	BEQ TURN_OFF_TRACK_BUFFERING
	CMP #'5 
	BEQ CALL_SUCCESS_CODE
	CMP #'6
	BEQ CALL_FAILURE_CODE
	CMP #'7
	BEQ STEP_5_TRACKS
	;
	; must be 8 - get drive #
	JMP GET_DRIVE_NUMBER

CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY
	JSR CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY_SUB
	JMP DISPLAY_MAIN_MENU

UPLOAD_COLDSTART_CODE
	JSR UPLOAD_COLDSTART_CODE_SUB
	JSR UPLOAD_CODE
	JSR CALL_UPLOADED_CODE
	JMP DISPLAY_MAIN_MENU
	
TURN_ON_TRACK_BUFFERING
	JSR TURN_ON_TRACK_BUFFERING_SUB
	JMP DISPLAY_MAIN_MENU

TURN_OFF_TRACK_BUFFERING
	JSR TURN_OFF_TRACK_BUFFERING_SUB
	JMP DISPLAY_MAIN_MENU

CALL_SUCCESS_CODE
	JSR UPLOAD_COLDSTART_CODE_SUB
	JSR UPLOAD_CODE
	JSR CALL_RETURN_SUCCESS_SUB
	JMP DISPLAY_MAIN_MENU
	
	
CALL_FAILURE_CODE	
	JSR UPLOAD_COLDSTART_CODE_SUB
	JSR UPLOAD_CODE
	JSR CALL_RETURN_FAIL_SUB
	JMP DISPLAY_MAIN_MENU

STEP_5_TRACKS

	JSR UPLOAD_COLDSTART_CODE_SUB
	JSR UPLOAD_CODE
	JSR CALL_STEP5_SUB
	

	; BPUT 0, FUNCTION_NOT_IMPLEMENTED_TEXT,FUNCTION_NOT_IMPLEMENTED_TEXT_LENGTH

	JMP DISPLAY_MAIN_MENU


;
; subroutines
;


CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY_SUB	 

	BPUT 0, CALLING_COLDSTART_DRIVE_TEXT,CALLING_COLDSTART_DRIVE_TEXT_LENGTH
	
	
	LDA	#< DD_FIRMWARE_COLDSTART
	STA	DAUX1	
	LDA	#> DD_FIRMWARE_COLDSTART		
	STA DAUX2
	LDA #'r							
	STA DCOMND
	
	LDA #SIO_NO_DATA
	STA DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #$2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
    
    BMI	ERROR_CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY
    
    RTS

ERROR_CALL_DRIVE_COLDSTART_ROUTINE_DIRECTLY

	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_CALL_CODE_DIRECTLY_TEXT, ERROR_CALL_CODE_DIRECTLY_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE
	
	RTS

 
    
UPLOAD_COLDSTART_CODE_SUB

		BPUT 0, SETTING_UPLOAD_CODE_ADDRESS_TEXT,SETTING_UPLOAD_CODE_ADDRESS_TEXT_LENGTH

;
; set load address to $2000 on the drive first	
;
	
	LDA	#< COMMAND_CODE_BUFFER
	STA	DAUX1	
	LDA	#> COMMAND_CODE_BUFFER
	STA DAUX2
	LDA #'s							
	STA DCOMND
	
	LDX #SIO_NO_DATA
	STX DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
    
    BMI	ERROR_SET_LOAD_ADDRESS
    RTS
;    JMP UPLOAD_CODE

ERROR_SET_LOAD_ADDRESS

	LDA DSTATS
	STA BIN_VAL

    BPUT 0, ERROR_SET_LOAD_ADDRESS_TEXT, ERROR_SET_LOAD_ADDRESS_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE

	RTS

   
   
; upload actual code

UPLOAD_CODE

		BPUT 0, UPLOADING_COLDSTART_CODE_TEXT,UPLOADING_COLDSTART_CODE_TEXT_LENGTH



	LDA	#<COMMAND_CODE_BUFFER
	STA	DBUFLO
	LDA	#>COMMAND_CODE_BUFFER
	STA	DBUFHI
	
	LDA #'u							
	STA DCOMND
	
	LDX #SIO_WRITE_DATA
	STX DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #2
	STA DTIMLO
	
	LDA #0
	STA DBYTLO
	LDA #1
	STA DBYTHI
	
    JSR	SIOV
   
    BMI	ERROR_UPLOAD_CODE
   
    RTS

ERROR_UPLOAD_CODE
 	 
	LDA DSTATS
	STA BIN_VAL

     BPUT 0, ERROR_UPLOAD_CODE_TEXT, ERROR_UPLOAD_CODE_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE

	RTS

;	JMP CALL_UPLOADED_CODE    ; Carry on processing, even if upload code errored out


    
 ; call code on drive

CALL_UPLOADED_CODE

		BPUT 0, CALLING_UPLOADED_CODE_TEXT,CALLING_UPLOADED_CODE_TEXT_LENGTH



	LDA	#< [DD_COLD_START_CODE - 1] ; COLDSTART - 1
	STA	DAUX1	
	LDA	#> [DD_COLD_START_CODE - 1]
	STA DAUX2
	LDA #'r							
	STA DCOMND
	
	LDX #SIO_NO_DATA
	STX DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #$2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
   
    BMI	ERROR_CALL_UPLOADED_CODE

	RTS   



;	JMP DISPLAY_MAIN_MENU    
 
 
ERROR_CALL_UPLOADED_CODE
 
	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_CALL_UPLOADED_CODE_TEXT, ERROR_CALL_UPLOADED_CODE_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE

	RTS

;	JMP DISPLAY_MAIN_MENU    
 
 
 
TURN_ON_TRACK_BUFFERING_SUB

; tested and works without error on a Disk Duplicator 1050

		BPUT 0, TURN_ON_BUFFERING_TEXT,TURN_ON_BUFFERING_TEXT_LENGTH

	LDA	#0		; turn on track buffering
	STA	DAUX1	
	LDA #'b						
	STA DCOMND
	
	LDX #SIO_NO_DATA
	STX DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
 
	BMI ERROR_TURN_ON_TRACK_BUFFERING


	RTS

ERROR_TURN_ON_TRACK_BUFFERING
	
	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_TURN_ON_TRACK_BUFFERING_TEXT, ERROR_TURN_ON_TRACK_BUFFERING_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE

	RTS
	


	
	
TURN_OFF_TRACK_BUFFERING_SUB

; tested and works without error on a Disk Duplicator 1050


		BPUT 0, TURN_OFF_BUFFERING_TEXT,TURN_OFF_BUFFERING_TEXT_LENGTH

	LDA	#1		; turn off track buffering
	STA	DAUX1	
	LDA #'b						
	STA DCOMND
	
	LDX #SIO_NO_DATA
	STX DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #20
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
 
 	BMI ERROR_TURN_OFF_TRACK_BUFFERING

	RTS	

ERROR_TURN_OFF_TRACK_BUFFERING
	
	LDA DSTATS
	STA BIN_VAL
 
    BPUT 0, ERROR_TURN_OFF_TRACK_BUFFERING_TEXT, ERROR_TURN_OFF_TRACK_BUFFERING_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE

	RTS


CALL_RETURN_SUCCESS_SUB	 

	BPUT 0, CALLING_SUCCESS_TEXT,CALLING_SUCCESS_TEXT_LENGTH
	
	
	LDA	#< [DD_RETURN_SUCCESS_CODE - 1]
	STA	DAUX1	
	LDA	#> [DD_RETURN_SUCCESS_CODE - 1]		
	STA DAUX2
	LDA #'r							
	STA DCOMND
	
	LDA #SIO_NO_DATA
	STA DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #$2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
    
    BMI	ERROR_CALL_RETURN_SUCCESS_DIRECTLY
    
    RTS

ERROR_CALL_RETURN_SUCCESS_DIRECTLY

	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_CALL_SUCCESS_CODE_TEXT, ERROR_CALL_SUCCESS_CODE_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE
	
	RTS

CALL_RETURN_FAIL_SUB	 

	BPUT 0, CALLING_FAIL_TEXT,CALLING_FAIL_TEXT_LENGTH
	
	
	LDA	#< [DD_RETURN_FAIL_CODE - 1]
	STA	DAUX1	
	LDA	#> [DD_RETURN_FAIL_CODE - 1]		
	STA DAUX2
	LDA #'r							
	STA DCOMND
	
	LDA #SIO_NO_DATA
	STA DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #$2
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
    
    BMI	ERROR_CALL_RETURN_FAIL_DIRECTLY
    
    RTS

ERROR_CALL_RETURN_FAIL_DIRECTLY

	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_CALL_FAIL_CODE_TEXT, ERROR_CALL_FAIL_CODE_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE
	
	RTS
 
CALL_STEP5_SUB

	BPUT 0, CALLING_STEP5_TEXT,CALLING_STEP5_TEXT_LENGTH
	
	
	LDA	#< [DD_STEP_5_TRACKS - 1]
	STA	DAUX1	
	LDA	#> [DD_STEP_5_TRACKS - 1]		
	STA DAUX2
	LDA #'r							
	STA DCOMND
	
	LDA #SIO_NO_DATA
	STA DSTATS
	
	LDA #$31						; SIO code for disk drive
	STA DDEVIC
	
	LDA DRIVE_NUMBER
	STA DUNIT
	
	LDA #$20						; longer timeout due to moving drive head
	STA DTIMLO
	
	LDA #$80
	STA DBYTLO
	LDA #0
	STA DBYTHI
	
    JSR	SIOV
    
    BMI	ERROR_CALL_STEP5_DIRECTLY
    
    RTS

ERROR_CALL_STEP5_DIRECTLY

	LDA DSTATS
	STA BIN_VAL
 
     BPUT 0, ERROR_CALL_STEP5_CODE_TEXT, ERROR_CALL_STEP5_CODE_TEXT_LENGTH

	JSR PRINT_SIO_ERROR_CODE
	
	RTS

 

;
;
; SIO_ERROR									; display a message saying SIO error (stored in Y) has occurred, 
;											; and wait for confirmation to continue
; 
; Y contains SIO Error
;

PRINT_SIO_ERROR_CODE

; convert to error code

	LDA #0
	STA BIN_VAL+1

	JSR BINBCD16

	LDA BCD_VAL
	CLC
	AND #$0F
	TAY
	LDA DECIMAL_TO_SCREEN,Y
	STA FILE_ERROR_TEXT+2
	
	LDA BCD_VAL
	AND #$F0
	CLC
	ROR 
	ROR 
	ROR 
	ROR 
	
	TAY
	LDA DECIMAL_TO_SCREEN,Y	
	STA FILE_ERROR_TEXT+1

	LDA BCD_VAL+1
	CLC
	AND #$0F
	TAY
	LDA DECIMAL_TO_SCREEN,Y
	STA FILE_ERROR_TEXT

	BPUT 0,SIO_ERROR_CODE_TEXT,SIO_ERROR_CODE_TEXT_LENGTH

	BPUT 0,FILE_ERROR_TEXT,3

	RTS

;
;
;

;
	
;
; (DOS) START ADDRESS 
; 
	*= $02E0
	.WORD PROGRAM_INIT
		
